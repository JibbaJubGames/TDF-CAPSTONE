using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class WeaponUpgradeInfoScript : MonoBehaviour
{
    public Image chosenWeapon;
    public TMP_Text chosenWeaponName;
    public TMP_Text chosenWeaponLevel;
    public TMP_Text chosenWeaponStatChanges;

    public TMP_Text chosenWeaponMats;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
